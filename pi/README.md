# Pi Extensions

This directory contains my in-progress pi extensions. I'm building them into my dotfiles so I have them whenever I set up a new workspace. They live here and for now I'm thinking of just keeping them here and installing them from the `~/dotfiles` directory instead of putting them somewhere and re-installing them.
